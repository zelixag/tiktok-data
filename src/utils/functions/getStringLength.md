获取字符串的长度（中文字符占两个字节）

| 参数 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| str | 字符串 | String | - |

返回结果 `Number` 类型

```js
import getStringLength from 'commons.js/functions/getStringLength'

getStringLength('我的123') // 7 

```